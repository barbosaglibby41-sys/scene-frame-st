# SceneFrame v0.1：SillyTavern 实机验证

> 当前是独立开发原型。请先在测试角色卡/测试聊天中验证，避免直接对常用聊天开启自动生成。

## 临时安装

将整个 `scene-frame` 文件夹复制到：

```text
SillyTavern/public/scripts/extensions/third-party/scene-frame/
```

然后重启 SillyTavern 或在扩展页重新加载。

## 基础检查

打开浏览器开发者控制台，输入：

```js
window.SceneFrame.diagnostics()
```

预期可看到：

- `eventSource: true` 或 `eventOn: true` 至少一个为 true；
- `messageNodes` 大于 0；
- `chatMessages` 在正常聊天有消息时大于 0。

## 检测测试

让 AI 或手动向一条助手消息加入：

```html
<image>1girl, solo, long hair, cafe, warm lighting</image>
```

预期：右下角出现 🖼️ 悬浮球；打开后在“检测到的图片块”看到对应卡片，并显示来源楼层。

## 手动生成测试

1. 保持“自动：关”。
2. 选择 A1111/Forge 或 NovelAI，填写地址；NovelAI 还需在本地界面填写 API Key。
3. 在检测卡片点“编辑”，检查 prompt 是否进入编辑框。
4. 点“生成”。
5. 完成后检查：图片是否插入原聊天楼层；若未插入，运行：

```js
window.SceneFrame.diagnostics()
```

并记录结果与浏览器控制台 `[SceneFrame]` 日志。

## 关键已知限制

- 插件保存 API Key 到该网站的 localStorage；不可将浏览器数据、插件设置或截图公开分享。
- A1111 从手机访问电脑的本地 `127.0.0.1` 无效；应填电脑局域网 IP，例如 `http://192.168.x.x:7860`，并确保 WebUI 允许局域网访问与 CORS。
- 自动模式尚未在真实酒馆事件中全面验证，建议先保持关闭。
