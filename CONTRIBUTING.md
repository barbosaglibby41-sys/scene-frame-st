# 开发约定

## 独立实现

本项目不复制、改写或拼接其他生图插件源码。可以参考公开文档、公开 API 规范和用户可见的产品功能，但实现、命名、界面和数据结构均独立设计。

## 提交前检查

- 不提交 API Key、Cookie、聊天导出和个人图片。
- 不把真实请求 URL、Authorization 头或响应中的密钥写入日志。
- 修改功能时补充测试或手动验收步骤。
- 移动端优先：触控区不小于 40px，避免依赖 hover。
- 生成任务必须可去重、可取消、可重试。

## Commit 建议

使用简短的 Conventional Commits：

- `feat:` 新功能
- `fix:` 修复问题
- `refactor:` 重构
- `docs:` 文档
- `test:` 测试
